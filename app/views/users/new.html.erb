<%provide(:title, 'Sign up')%>
<h1>Sign up</h1>
<div class="row">
  <div class="span6 offset3" >
    <%= form_for @user, url: { action: "create" } do|f|%>
      <%#= render 'shared/error_messages'%>

      <%= render 'fields', f: f%>

      <%= f.submit "创建帐号", class: "btn btn-large btn-primary"%>
    <% end %>
  </div>
</div>
